import 'reflect-metadata';

/**
 * Global initialization function
 * Called once when the app loads
 */
export default function () {
  // reflect-metadata is imported at module level (required for decorators)
  // This function can be used for other global initialization if needed
}
